# PostgreSQL Dockerfile for Render
FROM postgres:15-alpine

# Set default environment variables
ENV POSTGRES_USER=raven_user
ENV POSTGRES_DB=raven_shop_production

# Expose PostgreSQL port
EXPOSE 5432

# Use default postgres entrypoint
CMD ["postgres"]
